<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('_bootstrap_meta')%>
    <title>Sign Up</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f6f6; /* Light blue-gray background */
        }

        .container {
            max-width: 400px;
            margin: 50px auto;
            background-color: #ffffff; /* White background */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            color: #333333; /* Dark gray text */
        }

        input[type="text"],
        input[type="email"],
        input[type="password"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #cccccc; /* Light gray border */
            border-radius: 5px;
            box-sizing: border-box;
            margin-top: 8px;
            font-size: 16px;
        }

        .btn-success {
            background-color: #1b66e6; /* Green button */
            border: none;
            padding: 12px 20px;
            border-radius: 5px;
            color: #ffffff; /* White text */
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .btn-success:hover {
            background-color: #45a049; /* Darker green on hover */
        }

        .btn-login {
            color: #007bff; /* Blue login link */
            text-decoration: none;
            font-weight: bold;
        }

        .btn-login:hover {
            color: #0056b3; /* Darker blue on hover */
        }

        .form-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .form-header h4 {
            margin-bottom: 0;
            color: #333333; /* Dark gray text */
        }

        .form-footer {
            text-align: center;
            margin-top: 20px;
        }

        .form-footer p {
            margin-bottom: 0;
        }
    </style>
</head>
<body>
    <%- include('_header')%>
    <div class="container">
        <div class="form-header">
            <h4>Sign Up</h4>
        </div>
        <form action="/users/create" method="POST">
            <div class="form-group">
                <label for="name">Name</label>
                <input type="text" id="name" name="name" placeholder="Your name " required />
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" placeholder="Your mail" required />
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required />
            </div>
            <div class="form-group">
                <label for="confirmPassword">Confirm Password</label>
                <input type="password" id="confirmPassword" name="confirmPassword" required />
            </div>
            <div class="text-center">
                <button type="submit" class="btn btn-success btn-block">Sign Up</button>
            </div>
        </form>
        <div class="form-footer">
            <p>Already have an account? <a href="/users/signin" class="btn-login">Log in Instead</a></p>
        </div>
    </div>
    <%- include('_bootstrap_script')%>
</body>
</html>
